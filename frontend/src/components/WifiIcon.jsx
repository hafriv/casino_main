// Removed: consolidated WiFi styles into slider.scss and moved markup into OnlinePlayers.
// Keep a tiny stub export to avoid import errors in case any file still imports this module.
export default null;
